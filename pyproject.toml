[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "frame2d"
version = "0.1.0"
description = "2D 矩陣位移法(直接勁度法)通用框架分析"
requires-python = ">=3.9"
dependencies = [
    "numpy",
]

[tool.setuptools]
packages = ["frame2d"]
